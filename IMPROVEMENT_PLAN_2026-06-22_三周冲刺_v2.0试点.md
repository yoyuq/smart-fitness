# Smart Fitness — 三周冲刺规划书（迈向 v2.0 健身房试点）

> 制定日期：2026-06-22 · 团队：7 人 · 周期：3 周（15 个工作日）
> 基线：v1.0 闭环 + 06 月已完成（YOLO26 多人后端、评分V2四阶段、两种训练模式、AI 教练管家、UI 重设计 + 身体数据/目标/计划详情页、计划删除 bug 修复）
> 目标场景：**健身房多机位部署**（非家用单人）。本规划承接并刷新 `IMPROVEMENT_PLAN_2026-06-11_健身房部署.md`。

---

## 一. 三周总目标（North Star）

**3 周后交付一个可在真实健身房做小范围试点（5–10 机位）的 v2.0：**

> 多人同框稳 · 扫码即练 · 真实数据驱动评分 · 教练端可视 · 隐私合规达标 · 一键部署可监控。

衡量「成功」的硬指标见第六节 KPI。试点 = 在一家真实健身房铺 5–10 个机位、跑通 ≥3 天、采集 ≥500 条真实 rep、收集真实用户/教练反馈。

---

## 二. 现状盘点（截至 2026-06-22）

| 模块 | 已完成 | 仍是缺口（本次冲刺目标） |
|---|---|---|
| 多人识别 | YOLO26 后端已切换，主体锁定可行 | 真实场景跟踪鲁棒性、分类器在 17 点输入上置信度低 |
| 评分 | 规则 V2 四阶段、按 rep 评分、AI 评审团 | push_up/jumping_jack 结构性偏差需时序学习模型；**数据只有 ~58 条标注 rep，严重不足** |
| 训练模式 | 指导/完整两种模式 APP+后端全通、AI 报告 | 打磨与真实验证 |
| AI 教练 | 管家复盘 + 长期记忆 + 计划生成 | — |
| 客户端 | 登录/首页/训练/计划/我的 + 三个详情页 + 离线/推送 | **扫码绑定机位（QR）未做**、教练端未做、合规同意未做 |
| 设备 | 固件成熟（动态质量/差分/OTA/低功耗） | **AP 配网未做**（仍硬编码）、机队统一管理未做 |
| 规模 | 单 uvicorn + SQLite，51 req/s | **PostgreSQL/推理 worker 池未做**；>20 机位会顶 |
| 合规 | 原图 24h 清理、端侧不存图 | **PIPL 同意流程/删除权接口/留存审计未做** |
| 工程卫生 | Phase0 脱敏 + 归档完成 | git 历史泄露凭据未清、quant 项目仍混在仓库 |

---

## 三. 团队分工（7 人）

| 代号 | 角色 | 主战场 |
|---|---|---|
| **R1** | 后端·推理与扩展 | 推理 worker 池、PostgreSQL 迁移、并发与性能 |
| **R2** | 后端·业务与教练端 | 扫码绑定逻辑、机队管理 API、教练端后端、隐私接口 |
| **R3** | Android 客户端 | 扫码绑定、教练端视图、合规同意 UI、模式页打磨 |
| **R4** | ML / CV 算法 | 多人主体锁定鲁棒性、分类器重训、时序 AQA 模型 |
| **R5** | 数据工程·标注 | 采集回流管道、标注工具、真实测试集与基准 |
| **R6** | 嵌入式 | AP 配网门户、机队 OTA、每机位遥测上报 |
| **R7** | DevOps / QA / 合规 | 部署监控、压测、PIPL 合规落地、试点协调、演示 |

---

## 四. 分周计划

### 🟦 第 1 周：地基稳 + 多人准（Foundation & Robustness）
> 主题：把"能演示"变成"敢上真实场地"。先止血、再让多人同框真正可靠。

| 角色 | 本周任务 | 验收标准 |
|---|---|---|
| R1 | uvicorn 多 worker 化 + 推理拆独立 worker 雏形；建 PG 压测环境 | 同配置下并发吞吐较单进程 ≥2×；PG 可空跑 |
| R2 | 扫码绑定后端：二维码编码 device_id → 绑定/占用/idle 解绑/抢占冲突处理 | 两人扫同一机位有明确占用/排队结果，接口 200 |
| R3 | 集成扫码库（ZXing/CameraX），改造「绑定设备」为扫码即绑 | 扫器械区二维码 3 秒内完成绑定 |
| R4 | 多人主体锁定鲁棒性：IoU 跟踪 + 主体策略（中心+最大bbox+连续性），真实两人入镜实测 | 两人入镜骨架稳定锁定锻炼者，计数不被路人干扰 |
| R5 | 真实数据回流管道：训练 session 骨架序列（不存原图）自动归集落库 + 导出 | 一次训练后能导出该 session 全 rep 的 4 通道时序 |
| R6 | ESP32 AP 配网门户（WiFiManager + NVS 存储） | 一次编译的固件，新板上电进 AP 配 WiFi/Server 即可用 |
| R7 | Phase0 收尾：轮换全部泄露凭据、git 历史清理（git-filter-repo）、quant 拆独立仓 | 历史无明文密钥；仓库只剩 fitness 相关 |

**里程碑 M1（周五）**：新板子免编译配网 + 扫码即练 + 两人同框计数稳 + 仓库干净。

---

### 🟩 第 2 周：数据驱动 + 教练可视 + 合规（Data, Coach, Compliance）
> 主题：用真实数据替代合成数据，把"自练工具"升级成"教练能用的系统"，过隐私红线。

| 角色 | 本周任务 | 验收标准 |
|---|---|---|
| R4 | 用 R5 攒的真实数据重训 7 类分类器 + 训练时序 AQA 模型（push_up/jumping_jack 优先） | 真实测试集准确率有基准数；结构性偏差较规则下降 |
| R5 | **标注冲刺**：复用 session 回放 + AI 评审团预标，把带标注 rep 从 ~58 攒到 ≥400 | ≥400 条带时序标注 rep，每错误正负样本各 ≥30 |
| R2 | 教练端后端：多会员实时状态聚合 + 动作质量趋势 API；隐私接口（同意态、注销即删、留存审计） | 教练拉取本场地在练会员列表 + 各自评分 200 |
| R3 | 教练端视图（APP 或内嵌网页）+ 注册/训练前**知情同意**勾选 UI | 教练可见多会员实时卡片；未同意不可训练 |
| R1 | 完成 SQLite→PostgreSQL 迁移（沿用 migrate_v3 模式）+ 数据一致性校验 | 全功能在 PG 上回归通过 |
| R6 | 机队管理上报：每机位在线状态/帧率/延迟遥测 → 后端；批量 OTA 接口联通 | 后台能看到每块板在线/版本/帧率 |
| R7 | 评分规则 YAML 化（教练可改阈值不改代码）+ PIPL 合规清单（公示文案/留存策略自动执行审计） | 改 YAML 即生效；合规清单逐项可勾 |

**里程碑 M2（周五）**：真实数据训出的模型上线 + 教练端能看多会员 + 同意/删除合规闭环。

---

### 🟧 第 3 周：规模化 + 真实试点 + 发布（Scale, Pilot, Release）
> 主题：扛得住 20 机位，去真实健身房跑起来，打磨发版。

| 角色 | 本周任务 | 验收标准 |
|---|---|---|
| R1 | 推理 worker 池 + PG 正式上线，20 机位（40 req/s）压测调优 | 20 机位并发 p99 < 500ms，0 错误 |
| R7 | 监控完善：Prometheus/Grafana 每机位帧率/延迟/在线看板 + 告警 | 看板覆盖每机位；掉线/高延迟告警可触发 |
| R6 | 现场布点：5–10 机位上电配网 + 物料（二维码贴纸/支架）+ 联调 | 全部机位在线、扫码可绑 |
| R4+R5 | 试点期间持续采数 + 在线评估模型，必要时热修阈值 | 试点采集 ≥500 真实 rep，评分主观满意度回收 |
| R2+R3 | 试点护航：绑定/教练端/同意流程现场 bug 修复 | 试点 3 天无阻断性 bug |
| R7 | 演示视频 30s（F-08）、USER_GUIDE/部署文档刷新、v2.0 发版 + tag | 视频出片、文档更新、APK 与后端打 v2.0 标签 |

**里程碑 M3（周五）= v2.0 发布**：真实健身房试点跑通 ≥3 天，5–10 机位，采集 ≥500 真实 rep，出演示与复盘报告。

---

## 五. 关键依赖与物料

- **场地**：一家愿意配合试点的健身房（第 1 周内敲定，否则第 3 周试点顺延）
- **硬件**：5–10 块 ESP32-CAM + 支架/外壳 + 二维码贴纸；一台部署主机（建议 NUC/小服务器，PG+worker 池）
- **LLM**：现有 deepseek/火山 coding 配额（评分/报告/复盘链），注意配额与超时（readTimeout 已调 120s）
- **数据标注人力**：R5 主力 + 第 2 周可临时拉 1–2 人帮标（标注冲刺是本期最大不确定性）
- **真机授权**：试点用户知情同意（合规前置，R7 第 2 周必须就绪）

---

## 六. 验收 KPI（v2.0 Definition of Done）

| 维度 | 指标 | 目标 |
|---|---|---|
| 多人鲁棒 | 两人入镜锻炼者锁定正确率 | ≥95% |
| 评分质量 | 规则/AI 平均分差（真实数据） | ≤8（现 ~10.9） |
| 模型真实性 | 真实测试集分类准确率 | 建立基准（替代合成 98%）|
| 并发 | 20 机位 p99 延迟 / 错误率 | <500ms / 0 |
| 部署 | 新机位上线（配网→可练）耗时 | <5 分钟/台 |
| 合规 | PIPL 清单（同意/删除/留存/公示） | 100% 勾选通过 |
| 试点 | 真实运行天数 / 采集 rep 数 | ≥3 天 / ≥500 |

---

## 七. 风险登记表

| 风险 | 影响 | 缓解 |
|---|---|---|
| **标注数据攒不够** | 时序 AQA 模型训不出 → 评分仍靠规则 | 第 1 周就开管道；AI 评审团预标降人力；目标 400 达不到则保规则版兜底，模型作为 stretch |
| 试点场地谈不下来 | 第 3 周无法落地 | 第 1 周敲定；备选用自有/合作场地小规模替代 |
| git 历史清理影响协作 | 团队需重新 clone | 选周末做，提前通知，filter-repo 后统一基线 |
| PG 迁移引入回归 | 功能退化 | 第 2 周迁移 + 全量回归；保留 SQLite 回退路径 |
| USB/真机联调不稳（已知） | 调试效率 | 固定线缆、勾"一律允许 USB 调试"、备用无线 adb |
| 隐私合规滞后 | 阻断试点（法律红线） | 列为 M2 硬门禁，未过不试点 |

---

## 八. 明确不做（本期 Out of Scope）

- 边缘推理整机下沉（NUC 每区一台）→ 留待 >50 机位规模化阶段
- 社交对战/排行榜/战队、BLE 心率、商业化 SaaS 多租户 → v2.x 增值阶段
- 动作库大幅扩充（硬拉/卧推等）→ 等真实数据管道稳定后再加
- iOS 客户端

---

## 九. 每周节奏

- 每日 15 分钟站会（阻塞同步）
- 周五里程碑评审（M1/M2/M3 验收 + 下周对齐）
- 代码：功能分支 + PR 评审，禁止明文密钥入库（沿用 .env/NVS）
- 进度同步：本文件勾选 + daily memory 记一条

---

*本规划承接 v1.0 任务书（93% 完成）与 06-11 健身房部署规划（多人/评分/模式已落地），聚焦尚未闭环的「真实数据 · 规模化 · 教练端 · 合规 · 试点」五条主线。*
